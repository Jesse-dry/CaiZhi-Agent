"""
agents/ — AI Agent 层（LLM prompt 模板）

所有 agent 文件当前为占位空文件。
V2 阶段将接入 LLM，替换 services 中的关键词匹配引擎。

Agent 职责：
    qa_agent                  → 智能答疑（RAG + 知识图谱推理）
    mistake_diagnosis_agent   → 错题误区诊断
    socratic_agent            → 苏格拉底式引导问答
    feynman_agent             → 费曼解释评价
    graph_reasoning_agent     → 知识图谱因果链推理

不依赖 Streamlit。
"""

# 占位，等待 LLM 接入
